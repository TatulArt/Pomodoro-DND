/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.pravatar.cc'], // Потом нужно убрать это ПЛЕЙСХОЛДЕР СУЯЧАРААААААААААААА
      },
};

export default nextConfig;
